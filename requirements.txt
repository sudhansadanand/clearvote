# SGOA AGM Voting System - exact pinned runtime + test dependencies.
# No package is downloaded at runtime; install once before the AGM.
fastapi==0.141.1
starlette==0.52.1
uvicorn==0.41.0
pydantic==2.12.5
jinja2==3.1.6
python-multipart==0.0.22
reportlab==5.0.0

# Test-only
pytest==9.1.1
httpx==0.28.1
